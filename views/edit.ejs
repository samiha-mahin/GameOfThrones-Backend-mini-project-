<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white"> 
        <h1 class="text-3xl tracking-tighter mb-3">Update Profile</h1>
        <form action="/update/<%= users._id %>" method="post">
           <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" type="text" placeholder="name" name="name" value="<%= users.name %>">
           <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" type="text" placeholder="email" name="email"  value="<%= users.email %>">
           <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" type="text" placeholder="image url" name="image"  value="<%= users.image %>">
           <input class="px-5 py-2 text-black bg-yellow-500 rounded-lg m-3" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>